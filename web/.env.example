# Frontend environment variables (Vercel)
# Copy this to .env.local for local overrides (never commit .env.local)

# Leave blank in development — the Vite proxy handles /api → localhost:3001
# In production, set this to your Render backend URL (no trailing slash):
# e.g. https://metar-vibe-api.onrender.com
VITE_API_URL=
