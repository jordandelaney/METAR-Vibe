# Backend environment variables (Render)
# Copy this to .env for local overrides (never commit .env)

# Port is set automatically by Render — leave blank locally
PORT=3001

# Your Vercel frontend URL, e.g. https://metar-vibe.vercel.app
# Use * to allow all origins (fine during development)
FRONTEND_URL=*
